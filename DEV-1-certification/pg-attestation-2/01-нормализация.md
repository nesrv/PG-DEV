
Нормализация — это процесс организации структуры реляционной базы данных с целью уменьшения избыточности данных и предотвращения аномалий обновления, вставки и удаления.

Ниже кратко описаны **основные нормальные формы** (НФ), применяемые в процессе нормализации:

---

### **Первая нормальная форма (1НФ)**

**Требования:**

* Все атрибуты должны содержать **атомарные** значения (неделимые).
* Все записи (кортежи) таблицы — уникальны.
* Нет повторяющихся групп данных или массивов в одной ячейке.

**Пример нарушения:**

```text
Студент | Курсы
--------|---------------------
Иванов | Математика, Физика
```

**После приведения к 1НФ:**

```text
Студент | Курс
--------|----------
Иванов | Математика
Иванов | Физика
```

---

### **Вторая нормальная форма (2НФ)**

**Дополнительно к 1НФ:**

* Все неключевые атрибуты **зависят от всего первичного ключа**, а не от его части (отсутствие **частичных зависимостей**).

**Применимо только к таблицам с составными ключами.**

**Пример нарушения:**

```text
(Студент, Курс) → Оценка  
Курс → Преподаватель
```

Атрибут "Преподаватель" зависит только от "Курс", а не от полного ключа (Студент, Курс) — нарушение 2НФ.

**Решение — разбиение:**

* Таблица: (Студент, Курс, Оценка)
* Таблица: (Курс, Преподаватель)



Вот еще один **наглядный пример нарушения 2НФ** и его исправление:

---

### **Пример: Таблица "Записи об оценках студентов"**

**Исходная таблица:**

| **StudentID** | **CourseID** | **StudentName** | **CourseName**   | **Grade** |
| ------------- | ------------ | --------------- | ---------------- | --------- |
| 101           | CS101        | Иванов И.И.     | Программирование | A         |
| 101           | MA101        | Иванов И.И.     | Математика       | B         |
| 102           | CS101        | Петров П.П.     | Программирование | A         |

---

**Анализ:**

* **Ключ** таблицы: составной `(StudentID, CourseID)`
* `StudentName` зависит **только от `StudentID`**, а не от всего ключа
* `CourseName` зависит **только от `CourseID`**

> Это и есть **частичная зависимость**, а значит — **нарушение 2НФ**.

---

### **Приведение к 2НФ: разбиение таблицы**

Вот исправленный Markdown без экранирования символов, чтобы таблицы отображались корректно:

---

1. **Таблица `Students`**

| StudentID | StudentName |
| --------- | ----------- |
| 101       | Иванов И.И. |
| 102       | Петров П.П. |

2. **Таблица `Courses`**

| CourseID | CourseName       |
| -------- | ---------------- |
| CS101    | Программирование |
| MA101    | Математика       |

3. **Таблица `Grades` (связь между студентом и курсом)**

| StudentID | CourseID | Grade |
| --------- | -------- | ----- |
| 101       | CS101    | A     |
| 101       | MA101    | B     |
| 102       | CS101    | A     |

---

Если нужно, могу сгенерировать SQL-код создания этих таблиц и вставки 

Теперь:

* В таблице `Grades` нет частичных зависимостей: все атрибуты зависят **от всего составного ключа**
* База данных в **2НФ**






---

### **Третья нормальная форма (3НФ)**

**Дополнительно к 2НФ:**

* Все неключевые атрибуты зависят **только от первичного ключа**, а не друг от друга (отсутствие **транзитивных зависимостей**).

**Пример нарушения:**

```text
Студент → Группа  
Группа → Куратор
```

Значит, **Студент → Куратор** — транзитивная зависимость.

**Решение:**

* Таблица: (Студент, Группа)
* Таблица: (Группа, Куратор)

---

### **Нормальная форма Бойса-Кодда (НФБК / BCNF)**

**Уточнение 3НФ:**

* Для **каждой зависимости** X → Y, X должно быть **ключом**.

BCNF строго требует, чтобы **все детерминанты** были ключами.

**Пример нарушения (редкий случай):**

```text
(Преподаватель, Аудитория) → Время  
Время → Аудитория
```

* Время не является ключом, но определяет Аудиторию.
* Нарушение НФБК, даже если таблица в 3НФ.

**Решение — разбиение:**

* Таблица: (Время, Аудитория)
* Таблица: (Преподаватель, Время)





Вот краткое, но точное объяснение **четвёртой, пятой и шестой нормальной формы**, а также вводное описание **якорной модели данных**:

---

## ✅ **Четвёртая нормальная форма (4НФ)**

**Ключевая идея:**
Устраняет **многозначные зависимости** (multivalued dependencies), которые возникают, когда один и тот же ключ связан **независимо** с двумя и более множествами значений.

### 📌 Пример нарушения:

| Преподаватель | Курс       | Язык       |
| ------------- | ---------- | ---------- |
| Иванов        | Физика     | Русский    |
| Иванов        | Математика | Русский    |
| Иванов        | Физика     | Английский |
| Иванов        | Математика | Английский |

> Преподаватель Иванов ведёт два курса и знает два языка, **но курсы и языки никак не связаны между собой** — это два независимых множества.

📉 Повторение всех возможных комбинаций → **многозначная зависимость**.

### ✔ Решение:

Разделить на две таблицы:

* (Преподаватель, Курс)
* (Преподаватель, Язык)

---

## ✅ **Пятая нормальная форма (5НФ, или проектируемая нормальная форма)**

**Ключевая идея:**
Устраняет случаи, когда таблицу можно **разбить на несколько частей**, а потом **точно воссоздать только через соединение всех частей вместе**.

📌 Используется **редко**, касается **сложных зависимостей при разбиении на три и более таблиц**.

### 📚 Пример:

Компания привлекает **поставщиков** для **проектов** с использованием **деталей**. Зависимости между участниками можно разделить, но только **совместное соединение всех трёх таблиц** даст правильный результат.

> Если удалена — теряется информация. Если не нормализована — появляются **избыточные записи**.

---

## ✅ **Шестая нормальная форма (6НФ)**

**Ключевая идея:**
Максимально **разделяет данные по всем возможным независимым временным зависимостям**.

Используется в **базах с частыми временными изменениями**, например:

* Когда **один столбец** может меняться независимо от других (например, зарплата и должность сотрудника меняются в разное время).
* Используется в системах **хранилищ данных**, **бизнес-аналитике**, **temporal databases**.

📌 В 6НФ каждая таблица содержит **ровно одну зависимость + время действия записи**.

---



# Транзитивная связь в PostgreSQL

Транзитивная связь (transitive relationship) в контексте баз данных и PostgreSQL относится к ситуации, когда связь между сущностями передается через промежуточные элементы.

## Пример транзитивной связи

Рассмотрим пример с тремя таблицами, представляющими сотрудников, отделы и офисы:

```sql
-- Создаем таблицы
CREATE TABLE offices (
    office_id SERIAL PRIMARY KEY,
    office_name VARCHAR(100) NOT NULL
);

CREATE TABLE departments (
    dept_id SERIAL PRIMARY KEY,
    dept_name VARCHAR(100) NOT NULL,
    office_id INTEGER REFERENCES offices(office_id)
);

CREATE TABLE employees (
    emp_id SERIAL PRIMARY KEY,
    emp_name VARCHAR(100) NOT NULL,
    dept_id INTEGER REFERENCES departments(dept_id)
);
```

В этой структуре:
- Сотрудник (`employees`) принадлежит отделу (`departments`)
- Отдел принадлежит офису (`offices`)

Транзитивная связь здесь - это связь между сотрудником и офисом, которая осуществляется через отдел.

## Запрос с транзитивной связью

Чтобы найти, в каком офисе работает конкретный сотрудник, нам нужно пройти по транзитивной связи:

```sql
SELECT e.emp_name, d.dept_name, o.office_name
FROM employees e
JOIN departments d ON e.dept_id = d.dept_id
JOIN offices o ON d.office_id = o.office_id
WHERE e.emp_id = 123;
```

## Особенности транзитивных связей в PostgreSQL

1. **Производительность**: PostgreSQL эффективно обрабатывает транзитивные связи через соединения таблиц.

2. **Ограничения внешнего ключа**: Вы можете создать транзитивные ограничения, хотя PostgreSQL не поддерживает декларативные транзитивные ограничения напрямую.

3. **Рекурсивные запросы**: Для сложных транзитивных связей (например, иерархий) можно использовать рекурсивные запросы WITH:

```sql
WITH RECURSIVE employee_offices AS (
    -- Базовый случай: сотрудники и их отделы
    SELECT e.emp_id, e.emp_name, d.dept_id, d.office_id
    FROM employees e
    JOIN departments d ON e.dept_id = d.dept_id
    
    UNION ALL
    
    -- Рекурсивный случай: переход к офисам
    SELECT eo.emp_id, eo.emp_name, NULL, o.office_id
    FROM employee_offices eo
    JOIN offices o ON eo.office_id = o.office_id
)
SELECT * FROM employee_offices;
```

Транзитивные связи являются мощным инструментом для моделирования сложных отношений в базе данных.

